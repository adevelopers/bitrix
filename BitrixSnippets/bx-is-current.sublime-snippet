<snippet>
	<content><![CDATA[
  function is_current(\$dir)
  {
  	global \$APPLICATION;
  	if (strpos(\$APPLICATION->GetCurPage(),\$dir)!== false)
  	#if (\$APPLICATION->GetCurDir()==\$dir)
  	  	return true;
  	else 
  		return false;
  } 
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>bx-is-current</tabTrigger> 
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
